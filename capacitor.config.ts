import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tournaments-mgmt-webapp',
  webDir: 'www'
};

export default config;
